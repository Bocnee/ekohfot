// les composents react
import React from "react";
import { createRoot } from "react-dom/client";

// variables
const root = document.getElementById("root");

createRoot(root).render(
  <div>
    <h1>Salut</h1>
  </div>
);
